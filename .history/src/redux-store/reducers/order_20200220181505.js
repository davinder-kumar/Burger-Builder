import * as actionTypes from 
const initState = {
    loading : false,
    orders : []
}

const orderReducer = (state = initState, action) => {
    switch(action) {
        case : 
    }
}

export default orderReducer