import React from 'react'
import classes from './DrawerToggle.module'

const drawerToggle = (props) => (
    <div c onClick={props.clicked}>
        <div></div>
        <div></div>
        <div></div>
    </div>
)

export default drawerToggle